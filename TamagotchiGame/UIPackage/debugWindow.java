package UIPackage;
import javax.swing.JFrame;

public class debugWindow extends JFrame {

  // Frame >> panel
  public debugWindow() {
    JFrame f = new JFrame("debugWindow");
    f.setSize(300, 100);
    f.setVisible(true);
  }
}
